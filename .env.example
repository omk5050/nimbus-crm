# Not read by anything yet — the app runs entirely on mock data (src/mock/).
# Copy to .env.local and fill in once a real backend exists; see
# src/services/api.client.ts for exactly how these get consumed.

VITE_API_BASE_URL=https://api.nimbus-crm.example.com
VITE_APP_NAME="Nimbus CRM"
